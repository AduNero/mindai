# Core framework
Django==5.0.6
djangorestframework==3.15.1
django-environ==0.11.2

# Auth
djangorestframework-simplejwt==5.3.1

# Database
mysqlclient==2.2.4

# CORS / API docs / filtering
django-cors-headers==4.3.1
drf-spectacular==0.27.2
django-filter==24.2

# Async jobs (reminders, email, AI analysis triggers)
celery==5.4.0
redis==5.0.4
django-celery-beat==2.6.0

# Media / uploads
# Unpinned patch/minor (not an exact ==) so pip can resolve a build with a
# prebuilt wheel for whatever Python version is actually installed — image
# libs are usually the slowest to publish wheels for a new Python release.
Pillow>=10.3

# Input sanitization (admin-authored rich text in Resources)
bleach==6.1.0

# HTTP client — direct calls (apps.common.email_backends.ResendBackend,
# and apps.ai_engine's train_sentiment_classifier command fetching TweetEval).
requests>=2.31

# Sentiment classifier: TF-IDF + multinomial logistic regression, trained
# on TweetEval (see apps.ai_engine.management.commands.train_sentiment_classifier).
# Classical ML, not a deep-learning stack — light enough to live in base.txt
# rather than a separate AI-only requirements file.
scikit-learn>=1.4
joblib>=1.4

# WSGI server
gunicorn==22.0.0
