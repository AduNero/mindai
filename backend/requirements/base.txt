# Core framework
Django==5.0.6
djangorestframework==3.15.1
django-environ==0.11.2

# Auth
djangorestframework-simplejwt==5.3.1

# Google Sign-In — verifies the ID token client-side auth returns (local
# signature check against Google's cached public keys, no per-login
# network round-trip after the first fetch; Google's own guidance is to
# use this library rather than hitting their tokeninfo debug endpoint
# per-request in production).
google-auth==2.32.0

# AI chat companion — OpenAI-API-compatible client (see apps.chat.services.llm)
openai>=1.50

# Database
mysqlclient==2.2.4

# CORS / API docs / filtering
django-cors-headers==4.3.1
drf-spectacular==0.27.2
django-filter==24.2

# Async jobs (reminders, email, AI analysis triggers)
celery==5.4.0
redis==5.0.4
django-celery-beat==2.6.0

# Media / uploads
# Unpinned patch/minor (not an exact ==) so pip can resolve a build with a
# prebuilt wheel for whatever Python version is actually installed — image
# libs are usually the slowest to publish wheels for a new Python release.
Pillow>=10.3

# Report exports
reportlab==4.2.0

# Input sanitization (admin-authored rich text in Resources)
bleach==6.1.0

# HTTP client — direct calls (apps.common.email_backends.ResendBackend);
# already an indirect dependency via openai, pinned here since we now
# depend on it directly too.
requests>=2.31

# WSGI server
gunicorn==22.0.0
